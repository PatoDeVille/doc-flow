# 🚀 Document Processing Pipeline Configuration

# Server
PORT=3001  # Avoiding potential conflicts

# 🗄️ Redis Configuration  
REDIS_HOST=localhost
REDIS_PORT=6370  # Custom port to avoid your work setup
REDIS_PASSWORD=

# 🐘 PostgreSQL Configuration  
POSTGRES_HOST=localhost
POSTGRES_PORT=5440  # Custom port to avoid your work setup
POSTGRES_DB=docprocessor
POSTGRES_USER=admin
POSTGRES_PASSWORD=password

# 📦 MinIO Configuration (S3-compatible)
MINIO_ENDPOINT=localhost
MINIO_PORT=9010  # Custom port to avoid your work setup
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=documents
MINIO_USE_SSL=false

# 🔧 Processing Configuration
MAX_FILE_SIZE=10485760  # 10MB in bytes
ALLOWED_FILE_TYPES=application/pdf,image/jpeg,image/jpg,image/png
OCR_TIMEOUT=30000       # 30 seconds
PROCESSING_RETRY_ATTEMPTS=3
PROCESSING_RETRY_DELAY=5000  # 5 seconds

# 📊 Logging
LOG_LEVEL=info
LOG_FORMAT=combined

# testing
ENABLE_TEST_FAILURES=true
